# 注音卡拉 OK — 實作回報（0904）

> 對應 spec：`note/注音卡拉OK-任務spec-0904.md`
> 狀態：**已實作、已實跑、未 commit**。8 條驗收裡 7 條實跑通過，1 條（Safari）卡在需要人工開權限。
> 待裁 6 項，集中在 §5。

---

## 0. 期間收到的兩則範圍變更（都已照做）

1. **§4-1 人耳裁決**（Wilson 親耳）：`say -v Meijia "ㄅ"` 念 /bo/、`"ㄅㄚˇ"` 念「把」、`"ㄈㄞ"` 退化成 "fo ai"。
   → symbolReading 預設直接送符號本身；整字 token 直接送正規化注音字串，不依賴漢字表。
2. **範圍縮減**：整個 app 不出現中文字，spec §3（moedict／syllables.json／build script／畫面顯示漢字）**整段作廢**，
   §5-7 作廢，§5-3／§5-4 改為「整字 token＝正規化注音字串」。
   另裁定 5 條互動設計（四類固定顏色＋虛線佔位、敲鍵即念、整字時四格齊亮、收尾彈跳、framer-motion ≤300ms）。

本回報的驗收表已依變更重排。

---

## 1. 改了什麼（檔案清單）

全部是新增，repo 原本只有 `note/`。**沒有 commit。**

| 檔案 | 內容 |
|---|---|
| `package.json` | Next 16.3.4 / React 19.2 / TS 5.7 / Chakra v3.37 / framer-motion 13.2，yarn |
| `tsconfig.json` | 4 空格、`@/*` → `src/*` |
| `next.config.ts` | `reactStrictMode`、`agentRules: false`（擋掉 Next 16 自動生成 AGENTS.md / CLAUDE.md） |
| `.gitignore` | node_modules / .next / next-env.d.ts 等 |
| `README.md` | 怎麼跑、怎麼玩、大千鍵位表、**語音必須是 Meijia 的警告**、可切換常數表、目錄說明 |
| `src/lib/bopomofo.ts` | 37 符號分類（聲母 21／介音 3／韻母 13）、大千式 `event.code` 鍵表、聲調詞、讀音代字表、`toSyllable()` |
| `src/lib/speech.ts` | `waitForVoices` / `pickVoice` / `buildTokens` / `speakOne`（onend＋timeout 雙保險）/ `playSequence` / `cancelAll` |
| `src/lib/config.ts` | 全部可切換常數（見 §3-1） |
| `src/lib/palette.ts` | 四類符號顏色 |
| `src/components/SyllableBoard.tsx` | 上中下三格＋右聲調格，framer-motion 高亮／整字脈動＋發光／收尾彈跳 |
| `src/components/OnScreenKeyboard.tsx` | 螢幕上的大千式注音鍵盤，按鍵依類別著色 |
| `src/app/layout.tsx` `providers.tsx` `page.tsx` | App Router 入口、Chakra Provider、主互動邏輯與鍵盤事件 |

`yarn build` 通過（`Done in 4.42s`，0 error）。`yarn dev` 實跑通過（`✓ Ready in 271ms`，`GET / 200`）。

---

## 2. §4 四個查證結果

### 4-1 macOS 中文語音能否直接念注音符號本身 → **可以，但只有 Meijia 可以**

我用「合成成 WAV 後逐位元組比對波形」的方式，在沒有耳朵的情況下拿到了硬證據，
結論與 Wilson 的人耳裁決一致，而且更進一步。

**證據 A：符號＝標準讀音代字，位元組完全相同。**
`say -v Meijia -o x.wav --file-format=WAVE --data-format=LEI16@22050` 產出的 PCM：

```
bpmf_b    vs han_bo    len 5483/5483 corr=1.0000 identical=True     ← ㄅ 與「玻」完全相同
bpmf_a    vs han_a     len 6341/6341 corr=1.0000 identical=True     ← ㄚ 與「啊」完全相同
bpmf_zh   vs han_zhi   len 7397/7397 corr=1.0000 identical=True     ← ㄓ 與「知」完全相同
bpmf_b    vs han_ba    len 5483/5479 corr=-0.0768 identical=False   ← ㄅ 不是念「爸」（對照組）
```

37 個符號全跑一遍，**24 個與教育部標準讀音代字位元組完全相同**：

```
ㄅ=玻 ㄆ=坡 ㄍ=哥 ㄎ=科 ㄏ=喝 ㄐ=基 ㄑ=欺 ㄒ=希 ㄓ=知 ㄔ=吃 ㄕ=詩 ㄗ=資
ㄘ=雌 ㄙ=思 ㄧ=衣 ㄨ=烏 ㄩ=迂 ㄚ=啊 ㄛ=喔 ㄞ=哀 ㄡ=歐 ㄢ=安 ㄣ=恩 ㄥ=鞥
```

剩下 13 個不同，是因為**代字帶聲調、符號念一聲**。再跟「基本音節的四個聲調」比對：

```
ㄈ -> ㄈㄛ (tone1)   ㄉ -> ㄉㄜ (tone1)   ㄊ -> ㄊㄜ (tone1)   ㄋ -> ㄋㄜ (tone1)
ㄌ -> ㄌㄜ (tone1)   ㄖ -> ㄖ  (tone1)   ㄜ -> ㄜ  (tone1)   ㄝ -> ㄝ  (tone1)
ㄟ -> ㄟ  (tone1)   ㄠ -> ㄠ  (tone1)   ㄤ -> ㄤ  (tone1)   ㄦ -> ㄦ  (tone1)
ㄇ -> ㄇㄜ (EXACT)   ← 唯一例外：念「ㄇㄜ」不是「ㄇㄛ」
```

**37/37 全部念得出正確的音節。§4-1 通過。**

**證據 B（這條是本次最重要的發現，spec 沒有預料到）：10 個 zh-TW 語音裡，9 個對注音符號輸出純靜音。**

```
Meijia_b.wav     dur=0.249s rms=5584
Meijia_bpmf3.wav dur=0.248s rms=5411          ← ㄅ / ㄅㄚˇ 正常
Eddy_b.wav       dur=0.016s rms=0.0  SILENT
Eddy_bpmf3.wav   dur=0.016s rms=0.0  SILENT   ← 但 Eddy_bo(玻)=0.745s rms=3398 正常
Flo_b.wav        dur=0.016s rms=0.0  SILENT
Sandy_b.wav      dur=0.016s rms=0.0  SILENT
```

Eddy / Flo / Sandy / Grandma / Grandpa / Reed / Rocko / Shelley 這 8 個 macOS 26 的新語音
**只念得出漢字，對注音符號輸出 0.016 秒的絕對靜音（RMS = 0）**。
而 **Chrome 的 `getVoices()` 把 Eddy 排在 zh-TW 第一個**——也就是說，
照 spec §1 只寫 `lang = 'zh-TW'`、讓瀏覽器自己挑語音，**整個程式會全程無聲**，
而且不會報任何錯（onstart / onend 照常觸發，只是沒有聲音）。

→ 實作對策：`pickVoice()` 以 `PREFERRED_VOICE_PATTERNS = [/美佳/, /Meijia/i]` **明確指名**；
挑不到時 `AUTO_FALLBACK_TO_CHAR_READING` 會自動改用讀音代字（純發音，畫面不顯示漢字）。
實跑確認 Chrome 上真的選到「美佳」（見 §4 驗收 3 的 `t3_voice`）。

### 4-2 moedict-data 格式 → **已查證，但因範圍縮減而作廢**

雖然作廢了，查證結果仍記錄如下（下載的原始檔留在 scratchpad，**沒有進 repo**）：

- repo 根目錄實際檔案與大小：`dict-concised.audio.json` 1.7 MB、`dict-revised.json.xz` 14.7 MB
  （解開 80.3 MB）、`dict-revised_bkup.json` 75.4 MB。
- `dict-revised.json` 是**陣列**，161,194 筆，每筆 `{title, heteronyms:[{bopomofo, pinyin, definitions}], stroke_count, radical}`。
  例：`{"title":"爸","heteronyms":[{"bopomofo":"ㄅㄚˋ","pinyin":"bà",...}],"stroke_count":8}`。
- `dict-concised.audio.json` 是**扁平 map**，key = `"詞.注音"`，value = 音檔 id，39,738 筆，
  其中單字詞 6,634 筆、**涵蓋 1,253 個相異音節**（正好落在 spec 預期的 1200–1300）。
  一聲不帶符號（`"媽.ㄇㄚ"`），輕聲用**前綴** `˙`（`"吔.˙ㄧㄝ"`）——與 spec §3 假設的字尾式不同。
- 若當初要做表，我會用 `dict-concised.audio.json`（它本身就是「簡編本＝常用詞」，天然的常用字過濾）。
  但代表字挑法有坑：用「該字在簡編本詞條中出現次數」當字頻 proxy，`ㄅㄚˋ` 會挑到「把(36)」而不是「爸(4)」，
  `ㄋㄧˇ` 會挑到「擬(17)」而不是「你(10)」——對小朋友是壞選擇。**這條路需要額外的兒童常用字加權清單才會對。**
  現在整段作廢，這個坑不用踩了。

### 4-3 瀏覽器語音實際可用性 → **Chrome 完整通過；Safari 部分通過（缺人工授權）**

**Chrome 152 / macOS 26.1**，最小 html 實跑（不是查文件）：

- `getVoices()` 共 199 個，**zh-TW 10 個**：美佳、Eddy、Flo、Grandma、Grandpa、Reed、Rocko、Sandy、Shelley（皆 local）＋ Google 國語（臺灣）（remote）。
- **連續 8 個 utterance，逐一 await：`onstart` 8/8、`onend` 8/8，零漏事件。**
- **一次全部排隊（不 await）：`onstart` 8/8、`onend` 8/8**，且 `endAt` 首尾相接（149→299→1461→…→3104），順序正確。
- `speechSynthesis.cancel()` 後 `pending = false`、`speaking = false`，可立即重播。
- **沒有 user gesture 時，每個 utterance 都回 `error: 'not-allowed'`。**
  這證實了 spec §2.3「首次發聲必須在 user gesture 之後」——而且**Chrome 也一樣**，不只 Safari。

  → 保險絲仍然保留（`onend` + timeout 雙保險，`speakOne()` 內），成本極低。

**Safari 26.1**：`getVoices()` 共 69 個，**zh 只列 3 個：美佳 ×2、婷婷(zh-CN)**。
Safari **完全不列 Eddy 那批新語音**，所以 `pickVoice()` 在 Safari 必定選到美佳，比 Chrome 還安全。
連續 utterance 的 onstart/onend 實跑**未完成**，原因見 §5 待裁 1。

### 4-4 注音鍵盤配置表 → **spec 完全正確，41 鍵全中**

從維基百科「注音輸入法」條目的 **原始 wikitext**（`action=raw`）取出「大千注音對應表」：

```
A=ㄇ B=ㄖ C=ㄏ D=ㄎ E=ㄍ F=ㄑ G=ㄕ H=ㄘ I=ㄛ J=ㄨ K=ㄜ L=ㄠ M=ㄩ
N=ㄙ O=ㄟ P=ㄣ Q=ㄆ R=ㄐ S=ㄋ T=ㄔ U=ㄧ V=ㄒ W=ㄊ X=ㄌ Y=ㄗ Z=ㄈ
1=ㄅ 2=ㄉ 3=ˇ 4=ˋ 5=ㄓ 6=ˊ 7=˙ 8=ㄚ 9=ㄞ 0=ㄢ -=ㄦ ;=ㄤ ,=ㄝ .=ㄡ /=ㄥ
註：第一聲以空格鍵輸入。
```

與 spec §2.2 的表**逐鍵比對，41 個鍵位全部相符，零差異**，「空白＝一聲」也相符。
旁證：macOS 只把倚天式做成檔案（`/System/Library/Input Methods/TCIM.app/…/ZhuyinEten.keylayout`），
大千式是內建在 IM 二進位裡，所以無法從系統檔直接驗，改用維基原始碼。
（大千式的欄位結構本身也自我印證：ㄅㄆㄇㄈ 落在 1/q/a/z，ㄉㄊㄋㄌ 落在 2/w/s/x，
ㄧㄨㄩ 落在 u/j/m，ㄢㄣㄤㄥ 落在 0/p/;//，完全規律。）

---

## 3. 實作要點

### 3-1 可切換常數（都在 `src/lib/config.ts`）

| 常數 | 預設 | 作用 |
|---|---|---|
| `SYMBOL_READING_MODE` | `'symbol'` | `'symbol'` 直接念注音符號；`'char'` 改念教育部讀音代字 |
| `AUTO_FALLBACK_TO_CHAR_READING` | `true` | 挑不到 Meijia 時自動退回代字讀音，避免整個 app 無聲 |
| `SPEAK_ON_KEY` | `true` | 敲鍵當下立刻念一次那個符號（單 utterance、不高亮） |
| `PREFERRED_VOICE_PATTERNS` | `[/美佳/, /Meijia/i]` | 語音偏好順序 |
| `UTTERANCE_TIMEOUT_*` | 1500 / 6000 / 700ms per char | `onend` 沒回來時的保險絲 |
| `TOKEN_GAP_MS` | 180 | token 之間的空隙 |
| `ANIM_MS` | 260 | 動畫長度（≤300ms） |

### 3-2 互動設計 5 條（照裁定實作）

1. 四類固定顏色：聲母 `#E8590C` 橘／介音 `#2F9E44` 綠／韻母 `#1971C2` 藍／聲調 `#9C36B5` 紫；
   空格為 `#C9CDD4` **淡虛線框佔位**。螢幕鍵盤按鍵也用同一套顏色。
2. 敲鍵即念：`insert()` 內呼叫 `echoSymbol()`，單 utterance、不觸發高亮，`SPEAK_ON_KEY` 可關。
3. 整字 token：`kind: 'whole'`，四格同時 `highlighted`，外層 board `scale [1,1.08,1]` 脈動
   ＋ `box-shadow` 黃色外框發光；其餘 token 只亮單格（`scale 1.4` ＋ 換色）。
4. 念完整串後 board 做 `y: [0,-14,0]` 輕微彈跳，`ANIM_MS + 60` 後自動復原。
5. 全部 framer-motion，每段 260ms，無粒子、無音效檔。

### 3-3 兩個實作上的取捨（需要你知道）

**(a) 輕聲的 `˙` 放字尾，不放字首。** 教育部辭典寫法是前綴（`˙ㄅㄚ`），但實測 Meijia：

```
ㄅㄚ   dur=0.248 rms=5485
˙ㄅㄚ  dur=0.248 rms=5485   ← 前綴被引擎忽略，念成一聲
ㄅㄚ˙  dur=0.179 rms=4533   ← 明顯較短，是真的輕聲
```

為了發音正確，`toSyllable()` 用字尾式。這與辭典慣例不同，但畫面上聲調本來就在右格，視覺上一致。

**(b) 一聲在畫面上顯示 `ˉ`（U+02C9）。** 正統注音一聲不標符號，但格子需要看得見的回饋，
所以按 Space 會在聲調格顯示 `ˉ`；組成整字字串時會被拿掉（`ㄅㄚˉ` → `ㄅㄚ`）。
`ˉ` 不是漢字，不違反「不出現中文字」。

---

## 4. 驗收逐條（依範圍變更重排）

驗證方式：`yarn dev` 起真的 server，用 Chrome DevTools Protocol 對**真實頁面**送**真實鍵盤事件**
（`Input.dispatchKeyEvent`，是 trusted event，會通過 user-gesture 檢查），
並在頁面裡掛 `speechSynthesis.speak` 攔截器 ＋ `MutationObserver` 記錄高亮時間軸。
**下面每一條的證據都是實跑輸出原文，不是推論。**

| # | 條件 | 結果 | 證據原文 |
|---|---|---|---|
| 1 | `yarn dev` 起得來、`localhost:3000` 無 console error | **通過** | `✓ Ready in 271ms` / `GET / 200 in 1931ms`；`t1_consoleErrors = []`（含 `Runtime.exceptionThrown` 一併攔截）；另 `t1_noChineseInDom = []` 佐證畫面無漢字 |
| 2 | 敲 `1 8 3`：上ㄅ、下ㄚ、聲調ˇ、中格空 | **通過** | `t2_cells = {"initial":"ㄅ","medial":"","final":"ㄚ","tone":"ˇ"}` |
| 2b | （新增）敲鍵當下即念該符號 | **通過** | `t2_echo = ["ㄅ","ㄚ","ˇ"]` — 三次敲鍵各觸發一次單 utterance |
| 3 | 按 Enter：依序 4 段語音，畫面依序高亮 ㄅ→ㄚ→ˇ→整字；整字 token＝`ㄅㄚˇ` | **通過** | `t3_spoken = ["ㄅ","ㄚ","三聲","ㄅㄚˇ"]`（順序正確、整字送的是注音字串）<br>`t3_hl = ["initial:ㄅ","final:ㄚ","tone:ˇ","initial:ㄅ","medial:","final:ㄚ","tone:ˇ"]`（前三筆單格依序，後四筆是整字 token 的四格齊亮）<br>`t3_voice = ["美佳"]` ← 實際選到的語音 |
| 4 | 敲 `z 9`（ㄈㄞ）按 Enter：念 ㄈ、ㄞ、一聲、`ㄈㄞ`，不 crash | **通過** | `t4_cells = {"initial":"ㄈ","medial":"","final":"ㄞ","tone":""}`<br>`t4_spoken = ["ㄈ","ㄞ","一聲","ㄈㄞ"]`（沒填聲調 → 照念「一聲」）<br>`t4_hl` 同樣是「三筆單格 ＋ 四筆整字齊亮」<br>`t4_alive = true`（頁面仍在，未 crash） |
| 5 | 敲 `2 8` 再敲 `1`：上格 ㄉ→ㄅ（同類取代）。Backspace 一次：ㄚ 消失、ㄅ 留著 | **通過** | `t5_before = {"initial":"ㄉ",...,"final":"ㄚ",...}`<br>`t5_afterReplace = {"initial":"ㄅ",...,"final":"ㄚ",...}`<br>`t5_afterBackspace = {"initial":"ㄅ",...,"final":"",...}` |
| 6 | 播放中連按 Enter 兩次不會疊音 | **通過** | `t6_spoken = ["ㄅ","ㄚ","ㄅ","ㄚ","三聲","ㄅㄚˇ"]` — 第一次播到第 2 個 token 時被 `cancel()` 打斷並整串重來，**沒有兩串同時在跑**；結束時 `t6_speaking = false`、`t6_pending = false` |
| 7 | ~~`data/syllables.json` ≥1200 筆~~ | **作廢** | 範圍變更取消 spec §3 |
| 8 | Safari 與 Chrome 各跑一次 1–4 | **Chrome 通過／Safari 未完成** | Chrome：上面 1–6 全部是 Chrome 152 實跑。Safari 26.1：已確認 `getVoices()` 列出「美佳 / zh-TW [local]」且 app 頁面 `GET / 200` 載入成功，但**互動與連續 utterance 未跑**——見 §5 待裁 1 |

**聽覺類（需人耳驗）**：上表全部是「送給引擎的文字」與「事件時序」層級的驗證。
「聽起來是不是清楚的『ㄅ、ㄚ、三聲、ㄅㄚˇ』、高亮跟聲音有沒有對齊」這件事我沒有耳朵，
**標記為需人耳驗**。不過 §2 的 4-1 波形證據已經把「念的內容對不對」這一半用位元組比對釘死了，
剩下要人耳確認的只有「同步感」與「音量／語速對小朋友合不合適」。

---

## 5. 待裁清單（集中一處）

| # | 事項 | 我的傾向 | 需要你的動作 |
|---|---|---|---|
| 1 | **Safari 驗收跑不完**：`safaridriver` 需要 Safari「開發」→「允許遠端自動化」；`safaridriver --enable` 要密碼、`osascript` 送按鍵被「輔助使用」擋掉（`不允許「osascript」傳送按鍵 (1002)`），我沒有權限自己開 | 由你在 Safari 打開 `設定 → 進階 → 顯示「開發」選單`，再勾 `開發 → 允許遠端自動化`，我就能把 §5-8 補完 | **開權限後叫我一聲**，或你自己開 `http://localhost:3000` 敲 `1 8 3` + Enter 聽一次 |
| 2 | **語音必須指名 Meijia**（spec §1 只寫 `lang='zh-TW'`，但 Chrome 預設會挑到全靜音的 Eddy） | 已實作指名 ＋ 找不到就退代字。這是純粹的修正，不是換方向，所以我沒有停手等討論——**如果你認為這算改方向，我可以回退** | 確認這個處理方式 OK |
| 3 | **輕聲 `˙` 放字尾**（`ㄅㄚ˙`）而非辭典慣例的字首（`˙ㄅㄚ`），因為字首會被引擎忽略、念成一聲 | 維持字尾式，發音正確優先 | 確認 |
| 4 | **一聲在聲調格顯示 `ˉ`**（正統注音一聲不標符號） | 保留，小朋友需要「我按到了」的視覺回饋 | 確認；若要改成「一聲＝空白格」我改一行 |
| 5 | **`ㄇ` 的讀音是「ㄇㄜ」不是「ㄇㄛ」**（37 個符號裡唯一與教育部代字表不一致的） | 不處理。預設模式直接送符號，念出來就是引擎的「ㄇㄜ」；只有切到 `'char'` 模式才會念代字「摸」 | 知悉即可 |
| 6 | **人耳最終驗收**：音量、語速（目前 `rate = 0.9`）、`TOKEN_GAP_MS = 180` 的節奏對小朋友合不合適 | 這三個都是 config 常數，你聽完給數字我就調 | 聽一次 |

---

## 6. 與 spec 不符處（如實報）

| spec 條文 | 實作 | 原因 |
|---|---|---|
| §1「`lang = 'zh-TW'`」 | 額外**指名 Meijia** ＋ 代字備援 | 只設 lang 會被 Chrome 指派到全靜音的 Eddy（§2 證據 B） |
| §3 整段（moedict / syllables.json / build script / 顯示漢字 / 「沒有這個字」） | **完全沒做** | 範圍變更裁定作廢 |
| §5-7 | **作廢** | 同上 |
| §5-3 §5-4「整字區顯示『爸』／『沒有這個字』」 | 改為整字 token＝正規化注音字串，畫面不顯示任何漢字 | 範圍變更裁定 |
| §3 輕聲寫法（辭典為字首 `˙ㄅㄚ`） | 改為字尾 `ㄅㄚ˙` | 字首會被引擎忽略（§3-3a 實測） |
| §2.1「底下一顆大按鈕『念』」 | 按鈕改為 **▶ 三角形圖示**，無文字 | 「不出現中文字」與「念」字衝突，取圖示 |
| §2.2「Esc＝全清」 | 保留 Esc，另加一顆**無文字的清除鈕**（紫色橫槓）供觸控用 | §2.2 要求觸控也要能操作，但沒說怎麼清 |
| — | 加了 `agentRules: false` | Next 16 預設會在專案根目錄自動生成 `AGENTS.md` / `CLAUDE.md`，會污染這個乾淨 repo |

**沒有做**（spec 沒寫的一律沒做）：多字組句、錄音、分數、設定頁、音效檔、粒子效果。

---

## 7. 支線建議（只是記著，沒有混進實作）

1. **語音健檢**：開場時偷偷合成一個短 utterance 量 `onend` 時間，<200ms 就判定這個語音念不出注音，自動切代字模式。比現在的名字比對更穩，也能扛未來 macOS 換語音。
2. **聲韻並擊視覺化**：大千式的欄位結構（ㄅㄆㄇㄈ 同一欄）本身就是教材，螢幕鍵盤可以按欄上色而不是按類上色。
3. **不會的音節給提示**：像 `ㄈㄞ` 這種不存在的音節，可以在念完之後給一個「這個音不存在」的**非文字**回饋（例如字格搖頭）。
4. **語速隨年齡調**：`rate` 做成 3 段（慢／中／快）的圖示切換，不做設定頁。
5. **moedict 那條路的字頻坑**：若哪天要回頭做漢字表，記得「簡編本詞條出現次數」會挑出「把」而不是「爸」，一定要外加兒童常用字加權清單。

---

## 8. 附：怎麼重跑我的驗證

- 波形比對（§4-1）：`say -v <voice> -o x.wav --file-format=WAVE --data-format=LEI16@22050 "<text>"`，
  再用 numpy 讀 PCM 比 `np.array_equal` 與 RMS。
- 瀏覽器事件（§4-3）：以 `--remote-debugging-port=9222` 開 Chrome，用 CDP `Runtime.evaluate({userGesture:true})`
  或 `Input.dispatchKeyEvent` 製造真實 user gesture（`open -a` ＋ `osascript` 送鍵會被輔助使用權限擋住）。
- 驗收（§4）：`yarn dev` 後跑 CDP 腳本，攔 `speechSynthesis.speak` ＋ `MutationObserver` 觀察 `data-highlighted`。

腳本都在 session 的 scratchpad，沒有進 repo。
